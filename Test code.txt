# Test code

cd

ls

git --version

# to clone code
# Set the commit name
git config --global user.name "Erik Hodges"

# to check the current user name
git config --global user.name

# to set up the email
git config --global user.email "erikhodges07@yahoo.com"

# to view the email address
git config --global user.email

# to clone a git repository
 git clone https://github.com/tamntn/list-of-all-things.git
 
 # to change working directory
 cd # space then hit tab to select the first letter of the folder

# git status
# git branch
# to go up a folder use cd ..

# to initialize a file use 
git init

# start by using "git init" to establish the repository on the local machine
# once repository is initialized, use "git add" to specify which files you want
# to attach to the repository

# 
# "git commit" is a local task.
# git commit requires a message (descriptor) using "git commit -m "descriptor here""
# "git push" is used to commit the changes to the hub



